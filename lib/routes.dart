import 'package:kanjiapp/home/home.dart';
import 'package:kanjiapp/training/training.dart';

var appRoutes = {
  '/home': (context) => const HomeScreen(),
  '/training': (context) => const TrainingScreen(),
};

// this is all just dummy shit for now 